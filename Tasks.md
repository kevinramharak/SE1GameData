
- generate a README.MD for the top directory, with table of contents
- For each data type (tables,libraries):
  - generate a `README.md` containing a table of contents with links to each specific markdown file
  - For each instance of that data type:
    - generate a `<data_type>/<library>/<id>_<name>.md` file
- generate back links for each nested item, for easier navigation
